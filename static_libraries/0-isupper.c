#include "main.h"
/**
 * _isupper - returns 1 if char is lowercase
 *
 * @c: character
 *
 * Return: if Lower 1 else 0
 **/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}

	return (0);
}
